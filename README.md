# cargo-purge
cargo-purge is a fast CLI tool for finding and cleaning Rust projects. It can scan the current directory or recursively detect Cargo.toml files, preview what will be cleaned, ask for confirmation, or force clean like rm -rf. Built with clean architecture and ready for parallel execution. 🦀
